import { RouteStub } from '@/components/sections/RouteStub';

export default function AboutPage() {
  return (
    <RouteStub
      index="02"
      title={['ABOUT']}
      milestone="Milestone 2"
      owner="Developer 1"
      scope="Brand story and photography, productions mode — maximalist but readable. Blocked on the client brand-story copy and photo set (BLOCKERS B2, B9)."
    />
  );
}
