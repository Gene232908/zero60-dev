import { RouteStub } from '@/components/sections/RouteStub';

export default function ServicesPage() {
  return (
    <RouteStub
      index="03"
      title={['SERVICES']}
      milestone="Milestone 2"
      owner="Developer 1"
      scope="The service lines as an animated bento layout with hover and reveal motion. Blocked on the final service descriptions for 063 Productions and 063 Society (BLOCKER B4)."
    />
  );
}
